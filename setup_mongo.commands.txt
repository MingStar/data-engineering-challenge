# Enable text search
use admin;
db.runCommand({setParameter:1, textSearchEnabled:true});


# Add indexes
use isentia;
db.news_articles.createIndex({'link':1});
db.news_articles.createIndex({'section': 'text', 'headline': 'text',
                              'author':  'text', 'keywords': 'text',
                              'text_summary': 'text'});